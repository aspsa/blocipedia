<h1>All Wikis</h1>
<% @wikis.each do |w| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
                <%= link_to w.title, w %>
            </h4>
        </div>
    </div>
<% end %>